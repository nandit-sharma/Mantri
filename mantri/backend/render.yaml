services:
  - type: web
    name: backend
    env: python
    buildCommand: pip install -r mantri/backend/requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
    plan: free
    rootDir: mantri/backend
